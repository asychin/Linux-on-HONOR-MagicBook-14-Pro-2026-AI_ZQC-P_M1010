# HONOR MagicBook Pro 16 2026 (XWC-P), board M1120.
#
# The I2C_DEVT table is byte-identical between ZQC-P and XWC-P, which is how the
# md5 check found it: the model was never the thing that mattered, the table
# was. The directory exists so somebody looking for XWC-P finds it, and points
# at the one copy.
name=SSDT I2C_DEVT with TPD0 and TPL1 given _CRS and _STA
same_as=zqc-p/M1010
status=reported
origin=Manjaro forum reports, byte-compared against this repository's table
