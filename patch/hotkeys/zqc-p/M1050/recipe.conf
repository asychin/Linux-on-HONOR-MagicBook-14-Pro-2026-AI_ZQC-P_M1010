# HONOR MagicBook Pro 14 2026 (AI), board M1050.
#
# The WMI table that carries the hotkey dispatcher is the same firmware on both
# boards: byte-identical except the runtime address it maps, checked against the
# ACPI dump in issue 11. So the codes are very probably these seven.
#
# Probably is not the same as captured, and this fix has never successfully
# installed on that machine: the run in issue 11 failed on the upstream tag bug
# before it got this far. A wrong code here makes a key do the wrong thing, not
# damage; patch/hotkeys/capture-keys.sh on that machine settles it.
name=HONOR Fn hotkey codes for huawei-wmi
same_as=zqc-p/M1010
status=reported
origin=issue 11: same WmiTable firmware, codes not captured there
