# HONOR MagicBook Pro 14 2026 (AI), board M1050.
#
# Only has anything to do once hotkeys works. It ran on this machine from the
# flat profile, before boards were split, and reported the performance key and
# the camera key; the camera id it acts on is this board's own 3277:00de.
name=Actions for the keys no desktop binds
seen=ran on this machine and reported both keys, in the issue 11 install log
status=reported
origin=issue 11, from this board's own install log
